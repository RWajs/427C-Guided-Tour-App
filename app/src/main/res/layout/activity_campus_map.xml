<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.ryan.queensutour.CampusMap"
    android:background="@drawable/purple_leather"
    android:gravity="center">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Google Maps View"
        android:id="@+id/buttonGoogleMapsView"
        android:background="@drawable/button_back"
        android:layout_marginBottom="@dimen/button_spacing"
        android:textSize="@dimen/main_button_text_size"/>


    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Picture View (Offline)"
        android:id="@+id/buttonPictureView"
        android:background="@drawable/button_back"
        android:layout_below="@id/buttonGoogleMapsView"
        android:textSize="@dimen/main_button_text_size"
        />

</RelativeLayout>
</ScrollView>